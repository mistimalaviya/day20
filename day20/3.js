let arr=['john','Alice','Bob','Amanda']

for(let x=0;x<arr.length;x++){

    if(arr[x].includes('A')){
        let ans=arr.indexOf((arr[x]))

        console.log(ans);
        break;
    }
}